{
  "name": "kirmatas",
  "version": "1.0.0",
  "description": "Kırmataş — tuğla kırma oyunu",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync android",
    "apk": "npx cap sync android && cd android && .\\gradlew.bat assembleRelease",
    "aab": "npx cap sync android && cd android && .\\gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.1.0",
    "@capacitor/core": "^7.4.3",
    "@capacitor/haptics": "^7.0.2",
    "@capacitor/local-notifications": "^7.0.7",
    "@capacitor/share": "^7.0.4",
    "@capacitor/status-bar": "^7.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
